{
  "name": "dev-version",
  "description": "echos branch, commit hash and whether the repo is dirty",
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.test.js | tap-spec",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "keywords": [
    "version",
    "git",
    "development"
  ],
  "author": "Remy Sharp",
  "license": "MIT",
  "dependencies": {
    "es6-promise": "^3.0.2",
    "then-fs": "^2.0.0"
  },
  "devDependencies": {
    "tap-spec": "^2.2.2",
    "tape": "^4.2.0",
    "semantic-release": "^4.3.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/remy/dev-version.git"
  }
}
